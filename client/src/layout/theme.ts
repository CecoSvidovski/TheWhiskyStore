import { createTheme } from "@mui/material/styles";

const appTheme = createTheme({
  palette: {
    primary: {
      main: '#313132',
    },
    secondary: {
      main: '#fbfbfd',
    },
  },
});

export default appTheme;